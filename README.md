Format while creating a PR title should be as following

feat/fix/breaking (JIRA TICKET NUMBER): commit message

breaking - major change (when we add a new feature into the project)

feat - minor change (updates in existing feature)

fix - patch (when we fix a bug in existing project)

example:

feat(AC-5): PROJ-123 add Razorpay
