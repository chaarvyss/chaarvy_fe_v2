export enum FieldType {
  TEXT = 'text',
  FIELD = 'field',
  SHAPE = 'shape',
  IMAGE = 'image',
  TABLE = 'table'
}

export enum ShapeType {
  RECTANGLE = 'rectangle',
  CIRCLE = 'circle',
  LINE = 'line'
}

export enum PageSize {
  A4 = 'A4',
  LETTER = 'Letter'
}

export enum Orientation {
  PORTRAIT = 'portrait',
  LANDSCAPE = 'landscape'
}
